## MyTerm 1.6.14（Build 139）

- 支持拖动 SSH、本地 Shell 和 Codex 标签的标题调整顺序；向左拖到目标标签前，向右拖到目标标签后，松开鼠标时完成移动。
- 拖放目标显示高亮边框；取消或移出标签栏不改变排序，关闭按钮不作为拖动起点。
- 保持原有会话对象、连接、输出和当前选中标签；快捷键轮换跟随新顺序。
- 拖放处理与排序逻辑独立封装，增加双向移动、会话身份保持、失效目标和非法载荷回归检查。

Apple Silicon 版本，临时签名，未进行 Apple 公证。
