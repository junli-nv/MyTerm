## MyTerm 1.6.15（Build 140）

- SSH 侧栏采用文件夹树展示，子会话缩进并显示层级引导线；分组及未分组可展开、折叠，搜索时自动展示匹配子项。
- 将一个未分组会话拖到另一个未分组会话上，可输入名称新建分组，一次保存两个成员；取消不改动数据。
- 保留拖入已有分组功能；拖到未分组标题或底部提示区可退出分组。拒绝自身、无效会话和已分组会话间的新建分组操作。
- 复用分组名称校验及原子保存，兼容原有分组文件；本次为分组与会话两层树，不新增嵌套子分组。

Apple Silicon 版本，临时签名，未进行 Apple 公证。
